<%= form_for(@use_case, :html => {:multipart => true}) do |f| %>
<% if @use_case.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@use_case.errors.count, "error") %> prohibited this use_case from being saved:</h2>
	<ul>
		<% @use_case.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>


<div class="field">
	<%= f.label :item %>
	<br />
	<%= f.text_field :item %>
</div>
<div class="field">
	<%= f.label :purpose %>
	<br />
	<%= f.text_field :purpose %>
</div>
<div class="field">
	<%= f.label :photo %>
	<br />
	<%= f.file_field :photo %>
</div>

<div class="actions">
	<%= f.submit %>
</div>
<% end %> 